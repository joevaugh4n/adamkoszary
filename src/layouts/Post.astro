---
import Layout from "./Layout.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;

// All styling here is handled by [slug].astro
---

<Layout title={title} description={description}>
  <slot />
</Layout>
