---
import Layout, { OGType } from "./Layout.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;

// All styling here is handled by [slug].astro
---

<Layout title={title} description={description} ogType={OGType.Article}>
  <slot />
</Layout>
